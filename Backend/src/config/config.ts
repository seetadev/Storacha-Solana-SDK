export const ADMIN_CONFIG: AdminConfig = {
  adminWallet: "InitialWallet",
  minimumDuration: 60 * 60 * 24 * 30, // initially kept as 30 days
  rate: 1, //Kept as 1 USDC per MB, units will be defined later
};
